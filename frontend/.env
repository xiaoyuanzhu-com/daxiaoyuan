# Tencent Maps JavaScript API key (qq.com/lbs).
# Public token, restricted by HTTP referer whitelist on the lbs.qq.com
# console (localhost + ddxy.xiaoyuanzhu.com). Safe to commit — it ends
# up in the built JS bundle regardless.
# Override locally via frontend/.env.local (gitignored) if needed.
VITE_TENCENT_MAP_KEY=GNSBZ-IGM6U-6CHVL-GP7Z4-RESJ3-DYBA5
